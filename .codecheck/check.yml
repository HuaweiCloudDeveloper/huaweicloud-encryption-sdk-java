version: 2.0

steps:
  pre_codecheck:
    - checkout

#工具参数
tool_params:
  secsolar:
    compile_script: mvn clean install -f pom.xml -Dmaven.test.skip=true -gs /usr1/maven/settings/cbu-maven-user-settings.xml -s /usr1/maven/settings/cbu-maven-user-settings.xml